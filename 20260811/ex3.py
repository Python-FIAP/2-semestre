def soma(a:float = 0, b:float = 0) -> float:
    """
    Soma de dois numeros
    :param a: primeira parcela
    :param b: segunda parcela
    :return: total
    """
    return a+b

print(soma(2, 3))
